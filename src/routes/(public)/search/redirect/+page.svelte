<script>
//   import { onMount } from "svelte";
//   import { goto } from "$app/navigation";
//   import { page } from "$app/stores";

//   let search = {}
//   onMount(() => {
//     $page.url.searchParams.forEach((value, key) => {
//       search[key] = value;
//     });
//     console.log("search", search);
//     // goto(`/search?date_start=${search.date_start}&date_end=${search.date_end}&age=${search.age}&license=${search.license}&pickup=${search.pickup}&dropoff=${search.dropoff}`)
//   });
</script>

<p-5>
    Loading...
</p-5>
<!-- 
<form action="/search" method="get">
    <input type="hidden" name="date_start" value={search.date_start} />
    <input type="hidden" name="date_end" value={search.date_end} />
    <input type="hidden" name="age" value={search.age} />
    <input type="hidden" name="licence" value={search.licence} />
    <input type="hidden" name="pickup" value={search.pickup} />
    <input type="hidden" name="dropoff" value={search.dropoff} />
</form> -->
